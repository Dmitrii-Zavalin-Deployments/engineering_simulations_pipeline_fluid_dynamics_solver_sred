[pytest]
pythonpath = src
addopts = -ra --strict-markers
testpaths = tests
markers =
    reflex: tests related to simulation reflex handling
    synthetic: tests using artificially generated field data
    thresholds: tests validating config or test threshold logic
    integration: higher-level tests across multiple modules



